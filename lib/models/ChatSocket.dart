/*
import 'dart:core';
import 'package:web_socket_channel/web_socket_channel.dart';

import '../apis/api.dart';
class ChatSocket {
   late String text;
   late String recipientID;
   late String senderID;
   late String endPoint;*/
  /*late  SocketIO socketIO;
   late List<String> messages;


   ChatSocket(this.text,this.recipientID,this.senderID);
   factory ChatSocket.fromJson(Map json) {
     return ChatSocket(json['senderID'], json['recipientID'], json['id']);
   }
   initiateChat(){
      socketIO=SocketIOManager().createSocket(endPoint,'/',);
      )
   }
   //sending message to the server and adding it to the list of messages
   sendMessage()
   {

   }
   getMessagesForChatID()
   {

   }

}*/